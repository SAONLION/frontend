# 이 파일을 `.env`로 복사한 뒤 값을 채운다. `.env`는 커밋하지 않는다.
#
# Vite는 `VITE_` 접두사가 붙은 값을 빌드 시 클라이언트 번들에 그대로 인라인한다.
# 따라서 여기에는 브라우저에 노출돼도 되는 값만 둔다. 비밀 키는 절대 넣지 않는다.

# 백엔드 API base URL.
# 배포 환경(Vercel)은 HTTPS로 서빙되므로 http:// 주소를 쓰면 브라우저가
# mixed content로 차단한다. 배포용 값은 반드시 https:// 를 사용한다.
VITE_API_BASE_URL=
